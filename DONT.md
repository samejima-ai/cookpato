# スコープ外定義

以下はこのプロジェクトのスコープ外とする。
**意図的に排除している**。AI能力の問題ではなく、「この機能があることがユーザー体験を悪化させる」と判断したもの。

---

## 意識的に排除する機能（既存献立アプリにあるが本アプリでは実装しない）

| 排除する機能 | 排除理由 |
|---|---|
| カテゴリ分け（主菜/副菜/汁物等） | 親子丼のように複数カテゴリにまたがるものを1分類に強制できない |
| 料理名のマスター登録・統一 | 表記ゆれを本人が気にしないため、強制すると手数が増える |
| レシピ登録・表示 | 既に把握している料理を作る用途であり、レシピは不要 |
| 栄養計算 | 計算に必要な入力が増えることで手軽さを失う |
| 買い物リスト自動生成 | ユーザーが手動で把握しているため不要 |
| 冷蔵庫の全在庫管理 | 既存の記憶方式で回っているため不要（作り置きストックは別機能として実装） |
| 家族・パートナー共有 | 単一ユーザー前提 |
| SNS共有 | 個人管理用途 |
| 写真添付 | 手軽さを損なう |
| 通知・リマインダー | 本人の記憶で運用できている |

---

## 実装が複雑になるため除外（AI能力の現状の限界）

- 創造的UXデザイン（アニメーション、独自UIコンポーネント）
- 複雑な状態管理UI（ドラッグ&ドロップでの献立並び替え、ピンチズーム等）
- パフォーマンス最適化（ただし無限スクロールの軽快さは機能要件として別途満たす）
- 多言語対応（日本語のみ）
- アクセシビリティの高度な対応（VoiceOver最適化等）

---

## 明示的に行わない通信・同期系

- クラウド同期・マルチデバイス同期
- サーバーAPIとのデータ通信（データは完全ローカル）
- プッシュ通知
- アカウント管理・ログイン機能

※ バックアップ／エクスポートは**ローカル完結の範囲で採用する**（SPEC.md「バックアップ」参照）。
2026-05-17 にクリップボード方式に全面改訂：エクスポートは `navigator.clipboard.writeText` で
JSON をコピー、復元は「ファイル復元（旧ファイル互換）」と「クリップボード貼り付け復元（新規）」の 2 経路。
ネットワーク通信・クラウド送信は行わない。
旧仕様（`<a download>` で iOS Files に蓄積 + 30 日経過のシマエナガバッジ催促）は本リリースで廃止。

※ PWA（Service Worker + manifest）は「ホーム画面追加」と「オフライン動作」の手段として**採用する**。
ただし通信を伴う用途（プッシュ通知、バックグラウンド同期等）には使わない。

---

## 将来スコープに入る可能性があるもの

以下は現時点で除外しているが、本人の要望次第で将来的に追加候補となる：

- 週次サマリー表示（「今週魚は何回？」等）
- タグ機能（本人が任意で付けられる、強制はしない）
  - 2026-04-16: 「お気に入りマーカー」として最小形で実装済（SPEC.md 参照）。
    マーカー表示のみで検索フィルタ・専用一覧は付けない。手軽さを損なわない設計を維持。
- 複数端末同期

ただし追加する場合も、「メモ帳の手軽さを損なわない」を最優先とする。
「あったら便利」レベルでは実装しない。

---

## 行・日付の操作に関する境界（F012/F013 追加に伴い明文化）

- **挿入（許容、F013）**: 任意位置に空行を「追加」する操作。既存データの位置を動かさない
- **スワップ（許容、F012）**: 日単位での双方向入れ替え。データ消失なし
- **並び替え（禁止）**: 既存料理行の上下移動・順序入れ替え
  - 理由：reorder は「どこに動かすか」の判断を求めるため **簡単** を損ねる。挿入とスワップで実用上のニーズはカバーできる
- **日単位の片方向移動・コピー・削除（禁止）**: スワップに一本化（誤操作時のデータ消失を防ぐ）

挿入は「位置指定の追加」、スワップは「両方向の入れ替え」と整理する。
既存データを動かす reorder は引き続き対象外とする。

---

## スコープ外の基本原則

> **使わない機能が画面にあること自体がユーザー体験を悪化させる。**

既存の献立アプリをやめた最大の理由は、機能過多による操作負担だった。
本アプリの競合は市販の献立アプリではなく「メモ帳」である。
メモ帳より手軽でなければ存在価値がない。

---

## 哲学による却下判断（INDEX.md / CLAUDE.md「哲学」セクション準拠）

新機能・UX 変更は **早い・簡単・便利** の 3 語に照らして判定する。
1 語でも損ねる変更は本ファイルに追記して却下する。
3 語の競合時は **「簡単」を優先**する（妻のメンタル負荷ゼロを最上位）。

| 却下した提案 | 損ねる語 | 却下理由 |
|---|---|---|
| 行のドラッグ&ドロップ並び替え | 簡単 | 「どこに動かすか」の判断を求める。挿入＋スワップで実用ニーズはカバー |
| 移動モード中の説明テキスト表示 | 簡単 | 視覚強調（青枠）で意図は伝わる。文言追加は読解負荷を増やす |
| 日付ごと移動の「上書き」モード | 簡単・便利 | スワップで足り、上書きは確認ダイアログを 1 枚増やすだけで何も嬉しくない |
| 長押しメニューのポップオーバー化 | 簡単 | iOS コンテキストメニューと混同される。既存パターン（行内アイコン置換）に統合する方が学習不要 |
| アンドゥ機能 | 簡単 | スワップは可逆操作なので「もう一度同じ操作」で戻る。アンドゥ UI を増やすほうがメンタル負荷高い |
| バックアップの Web Share API 経由（共有シート起動） | 簡単 | 共有シートの中断感が要件 R3 を毀損。クリップボードコピーで足りる（Council #33） |
| バックアップの OPFS 自動スナップショット | 便利 | OPFS は媒体内バックアップで真の保険にならない（PWA 削除と同時消失、localStorage と同じ脆弱性）。実装複雑度の割に効果が薄い（Council #33） |
| バックアップ催促 UI（30 日経過・シマエナガバッジ）の維持 | 簡単 | F012 でデータ消失リスクが下がった現状、催促は「使われない機能を常時画面に置く」状態。「忘れていい」設計に切り替える（Council #33） |
| 行末「＋追加」常設ボタン（2026-05-21） | 早い・簡単 | F013 `↓+`（末尾行長押し）で末尾追加と等価機能を提供済み。常設ボタンは「早い」を損ねる傾向（CLAUDE.md UI 規約）+ 機能重複は「簡単」を損ねる。過去日 / today+7 以降の空日への新規追加経路は失われるが、妻運用上ほぼ発生しないことを確認済（許容）。F013 設計時点（2026-05-17）の前提「末尾追加経路として＋追加を残す」は本サイクルで前提変更。**※2026-05-31 再評価**：妻報告「6/7 以降が書けない」で「未来空日の先行入力は発生しない」前提が覆ったため、**未来空日（today+7 以降）に限り入力導線を復活**。当初★1本で実装後、複数品入力の利便性からユーザー指摘で「＋マーク → タップで★5行展開」に変更。この ＋マークは**空日専用**（既存行がある日には出ない＝長押しメニュー `↓+` と機能重複しない）であり、廃止した「行末＋追加常設ボタン（既存行がある日の末尾に常設・`↓+` と重複）」とは別物。重複していた常設ボタンの却下は引き続き維持（SPEC「空日の入力可否」参照） |
