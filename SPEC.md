# 機能仕様

## WHY（目的）

妻は現在、スマートフォンのメモ帳アプリで月単位の献立計画を管理している。
メモ帳の以下の特性を気に入っている：

- 書くだけ（登録ボタンなし）
- 見るだけ（画面遷移なし）
- 1画面完結
- 自由記述（カテゴリ・料理名統一の強制なし）

一方、以下の3点に不満を抱えている：

1. 日付・曜日フォーマットを毎月手作業で作るのが面倒
2. 過去の献立をキーワードで検索できない（「豚バラ大根前いつ作った？何と出した？」が見えない）
3. 冷凍ストックのリストを月またぎで保持するのに、手動で次月メモへコピペしている

本アプリはこの3点だけを解決する。既存の献立アプリにある
「カテゴリ分け」「料理名統一」「レシピ管理」「栄養計算」「買い物リスト」等は
**意図的に実装しない**（DONT.md参照）。余計な機能は手軽さを損なう。

---

## WHAT（機能定義）

### カレンダー（無限スクロール）

#### 概要
日付と曜日が1行ずつ並んだ縦長のリストを、無限スクロールで過去・未来の方向に連続して閲覧できる。

#### 条件
- アプリ起動時、当日が画面中央付近に来るよう自動スクロールする
- 上スクロールで過去（前日・先月）、下スクロールで未来（翌日・来月）へ軽快に連続閲覧できる
- 月切替ボタンは配置しない（スクロールのみで移動）
- 各行は「M月D日（曜）」形式（例：`4月15日（火）`）
- 各行には自由記述の入力欄を1つだけ持つ
- 月の区切りには月ヘッダー（例：`── 2026年4月 ──`）を1行挿入する
- 日曜の行は視覚的に区別される（色または区切り線）
- 当日の行は背景色等で他の行と区別される
- スクロールによる端末負荷を最小化する（遠方の日の描画は遅延させてよい）
- スクロール中もデータは即座に保持されている状態とする（遅延ロードによる欠落は不可）

#### 優先順位
高

---

### 週達成「頑張ったね」表示

#### 概要
日曜〜土曜の 7 日間の献立がすべて埋まっている週に対して、
達成感を「累積カウンタ」と「達成演出」の 2 つで表現する。

#### 条件
- 週は**日曜始まり**（日〜土の 7 日）
- 「埋まっている」の定義は空状態応援表示と同じ
  （`lines.length === 0` でも `lines.length === 1 && lines[0].text === ""` でもない）
- 表示 1（累積カウンタ）：
  - カレンダー領域の**左下**に、金メダル（`week-medal.png`）× N の
    **フロートバッジ**を常駐表示（`absolute bottom-3 left-3`）
  - 「今日に戻る」ボタン（右下フロート）と左右対称・同型スタイルに揃える
  - 金メダルアイコンは `w-8 h-8`（他の一覧系アイコンより一回り大きく視認性重視）
  - `pointer-events: none`。編集・スクロールを阻害しない
  - 達成済み週の Sunday を `AppData.completedWeeks: DateKey[]` として永続化、
    カウントは `completedWeeks.length`
  - 一度入ったキーは（テキストを消しても）**減らない**（妻の「献血カウント」セマンティクス）
  - 同一 Sunday は重複追加しない
  - `count === 0` のときは**非表示**（フロートで 0 表記はノイズになるため）
  - 既存ユーザー遡及：`loadData` 時、保存値と現在 meals から導出した既達成週を union する
- 表示 2（達成演出）：
  - 「未達成 → 達成」に遷移した瞬間のみ、カレンダー中央に大きく
    `week-complete.png` をフェードイン → ふわっと浮遊 → フェードアウト（合計 3 秒）
  - サイズは**画面短辺の 85%** を 1 辺とする正方形（`w-[min(85vw,85vh)]`）。
    達成の瞬間だけ画面を占拠する「ご褒美スプラッシュ」感を狙う
  - 演出中もスクロール・編集を阻害しない（`pointer-events: none`）

#### 発火タイミング
- 評価は textarea の **`blur`（編集コミット）** 時に行う。
  キーストローク毎には評価しない（1 文字入力中の誤発火を防ぐため）
- 編集モード進入時の `meals` を baseline として保持し、
  blur 時に baseline と現在を比較して「未達成 → 達成」遷移を判定
- `completedWeeks` に既登録の週は再発火しない
  （達成済み週を編集し直したり、空にして再充填しても演出は出ない、カウントも変動しない）

#### 非対応
- 達成済み週を「今週いい感じ」のような画面を作って一覧しない
  - 理由：本人の静かな達成感に留め、外向きの実績化は意図しない
- カウントのリセット機能（積むのが目的）

#### 優先順位
低（ゲーミフィケーション寄りの装飾、運用本質ではない）

---

### 空状態の応援表示（ヘルパービジュアル）

#### 概要
これから先の献立が決まっていない日に、応援イラスト（シマエナガ）を行内に表示する。
「これから何作ろう」を前向きに捉えるための視覚的ヘルパー。
付けなくても全機能は成立する（純粋な装飾）。

> **応援表示（シマエナガ装飾）と入力導線（空行★プレースホルダ）は別概念**。
> 応援表示は本セクションのとおり today〜today+6 に限定するが、入力導線は **today 以降の全空日** に開いている
> （「行間挿入（F013）」§「空日の入力可否」参照）。両者を混同しない（2026-05-31 再評価で分離）。

#### 条件
- **今日を含む 7 日間**（today〜today+6）の空日にのみ表示する
- 過去日には表示しない（ウィンドウ起点を today にしているため自然に対象外）
- 今日が空日なら今日も対象に含む
- ウィンドウ外（today+7 以降）の空日は表示しない
- 「空日」の定義は、献立エントリがないか、1 行だけで内容が空文字のいずれか
- イラストは 2 秒周期で水平反転を交互に繰り返す（ぱたぱた演出）
  - アニメは装飾目的。アクセシビリティ上の意味は持たせない（`aria-hidden`）

#### 非対応
- 複数ウィンドウを連鎖させて遠い未来の空日まで拾う挙動は取らない
  - 理由：画面全体に応援マークが散らばると「未計画が大量にある」プレッシャーになり、
    本来の応援意図と逆効果になる
- 検索フィルタ・専用一覧など、空日を**機能的に操作**させる UI は提供しない

#### 優先順位
低（純粋な装飾・UX ヘルパー）

---

### フリー入力

#### 概要
各日の入力欄に、料理名・食材名・メモを自由に書ける。制約を課さない。

#### 条件
- 1日あたりの料理行は複数（1 行 = 1 品）。空行も保持可能（自動生成 4 空行を含む）
- 各行は表示専用（左カラム ★ または ✓ + 料理名 + 🛒 + ❤）。**行単位の編集はフロート入力フォーム（F011）経由で行う**
- 行をタップするとフロート入力フォームが起動し、その行の text を編集できる
- 入力内容は即時保存される（保存ボタン・登録ボタンなし）
- 入力中・入力後にカテゴリ選択や料理名統一を求めない
- 入力内容の表記正規化は行わない（ユーザーの表記をそのまま保存・表示）
- 行の順序はユーザーの入力順を保持する（自動並び替えしない）
- 既存行の並び替え（順序入れ替え）機能は提供しない。**任意位置への挿入**は F013 で提供する
  - 「挿入は位置を指定した追加であり既存データを動かさない」「並び替えは既存データの順序変更」と境界を区別する

#### ちょいメモ（料理行とは別枠の自由記述）
- 各日の日付ラベル直下に、料理行とは独立した1行のメモ欄を持つ（例：「遅くなる」「外食」「弁当」）
- 検索・完了トグル・お気に入り・週達成判定の対象外
- 表示専用。タップでフロート入力フォーム（F011）が起動し、メモを編集できる
- 即時保存（保存ボタンなし）
- 日付列幅（96px）に収まらない場合はフォントサイズを動的縮小（下限 8px）

#### 編集導線（2026-05-13 改訂）
- 旧モデル（textarea で全行一括編集）は撤去
- 行・メモのタップでフロート入力フォーム（F011）が起動し、対象の text を編集
- 妻が「リストを見ながら入力したい」要求に応えるため、編集中もカレンダーが見えるよう上部固定フロートに分離する
- IME 多重反映対策（uncontrolled + 再マウント）はフロート入力フォーム側で踏襲する
- 行の長押し（500ms）は引き続き削除モード進入のトリガー

#### 優先順位
高

---

### フロート入力フォーム（F011）

#### 概要
料理行・ちょいメモの編集を、画面上部に固定されたフロート入力フォームで行う。
旧モデル（行の textarea 全行一括編集 / メモの行内 input）はリストエリアに input を抱え込んでおり、編集中はリスト全体が押し下げられて妻の「リストを見ながら考える」体験を阻害していた。
本機能はリスト本体を完全に表示専用にし、編集は画面上部の独立フロートに移すことで、編集中もリストの全体像が見渡せるようにする。

#### 条件

##### 表示・配置
- 画面の `header` 直下に **`position: sticky`（または `fixed`）** で固定配置
- `EditingTarget` が `null` のときは完全非表示（DONT.md「使わない機能を画面に置かない」原則に従う）
- `EditingTarget` がセットされた時のみ slide-down 200ms で出現
- フロート閉時は即時非表示（DOM から消す）。閉アニメは行わない
  - 採用理由：閉アニメは妻のメンタル上「閉じた → でも見える」の中間状態を生むだけで実用上の価値が薄く、実装複雑度（DOM 消去前のアニメ管理）に見合わない
  - 出現アニメ（slide-down）のみ採用、CLAUDE.md「アニメは 100-200ms 以内」準拠の 200ms 上限

##### `EditingTarget` の型と意味
```
EditingTarget = { kind: "line", dateKey: DateKey, lineIndex: number }
              | { kind: "memo", dateKey: DateKey }
              | null
```
- `kind: "line"`：料理行の編集。対象は `meals[dateKey].lines[lineIndex]`
- `kind: "memo"`：ちょいメモの編集。対象は `meals[dateKey].memo`
- `null`：フロート非表示

##### 入力 UI
- 単一の 1 行 `<input type="text">` を内蔵
- **uncontrolled** で `defaultValue` に対象の現状値を渡す。`key={EditingTarget の識別子}` で対象切替時に再マウント
- IME 対応：`useComposition` を使用し、`compositionStart` 中の onChange は親 state を更新しない（F002 既存パターン踏襲）
- プレースホルダ：`kind: "line"` → 「料理名」、`kind: "memo"` → 「メモ」
- 自動 focus：`EditingTarget` セット時に input へ自動 focus

##### 確定・キャンセル経路
- **確定**：blur / Enter キー → `useAppData` の `updateLineAt` または `setMemo` を呼び出し、`EditingTarget = null`
- **キャンセル**：Escape キー → 何も反映せず `EditingTarget = null`
- 外タップでの確定／キャンセルは採用しない（誤タップ時の意図不明確を避ける）

##### 視覚フィードバック
- 編集中の対象行・対象メモには薄い背景色（`bg-yellow-50` 等）でハイライト
- 「どれを編集しているか」を妻が見失わないようにする

##### キーボード（iOS Safari）対応
- input の focus で virtual keyboard が下から起動する
- フロートは画面上部 sticky なので keyboard に被らない（visualViewport API は使わない、iOS デフォルト挙動に任せる）
- IME 多重反映バグは uncontrolled + 再マウントで対処（F002 既存対策と同等）

##### 編集中の行表示状態
- 行・メモ自体は引き続き表示（タップ済みのまま視認可能）
- 編集中も完了トグル・お気に入り・cart ボタン等は引き続き操作可能（フロートとは独立した経路）
- ただし「編集中の行をもう一度タップして編集モード解除」は不採用（タップは別の行へ編集対象を切り替える挙動）

#### 非対応
- フロート内での履歴検索結果表示・候補表示等は実装しない（フロートは 1 行 input に限定）
- マルチライン編集（textarea）への切替は実装しない（料理行・メモはいずれも 1 行で完結する）
- フロート位置の変更（下部表示等）は実装しない（妻のメンタル固定のため上部 sticky で確定）

#### 優先順位
高（F002 編集導線の主経路となるため）

---

### 完了トグル（品単位）

#### 概要
その日の献立の各品（各行）について「作った／まだ」を個別にワンタップで切り替える。
ストック料理中心の運用で、作りながら品ごとに完了を記録する用途。

#### 条件
- 各行（品）の行頭にトグルUI（チェックボックスまたはタップ可能な小アイコン）を1つ配置する
- タップで完了／未完了が即切り替わる
- 完了した行は打ち消し線または色変化で区別する
- トグルは**品単位**（行単位）。日単位のまとめトグルは配置しない
- 行の追加・削除・**任意位置への挿入**ができる（行間挿入の詳細は F013 参照）。
  既存行の **並び替え（順序入れ替え）は実装しない**
  - 「挿入」は位置を指定した追加であり、既存データの移動を伴わないため reorder と区別する
- 編集（テキスト変更・行追加・行削除）によって完了状態がリセットされても構わない
  - 理由：ユーザーは完了済みの品を後から編集する運用をしない
  - 実装上は「行番号と完了状態の配列」で管理してよい（行内容の変更追跡は不要）
- 新規追加された行の初期状態は未完了
- スクロール・アプリ再起動後も完了状態は保持される

#### 調理中操作の最適化
- **ヒット領域**：トグル／お気に入り／削除のいずれも 44×44px を下限とする（iOS HIG 準拠）
  - 理由：調理中のぬれ手・油汚れ手タップでも誤爆しない最小サイズを確保する
  - 以前は「行左 1/3 以上を占有」としていたが、料理名の表示幅を圧迫したため廃止。44×44px の正方形ヒット領域で十分ターゲットできると判断
- **視覚フィードバック**：文字色グレー + チェックボックスのグレー塗り
  - 理由：完了行は視覚的に後退させ、未完了行との**相対的なコントラスト**で識別させる。調理中は「次に作るもの」を主眼にするため、完了済みは目立たせない方針
- **触覚フィードバック**：対応端末では完了/お気に入り/削除タップ時に `navigator.vibrate` で短く振動
  - iOS Safari は `vibrate` 非対応のため no-op となる（それで問題ない）
- **チャタリング防止**：300ms 以内の連続タップは 1 回として扱う
  - 理由：調理中の震え手で意図せず二重タップが発生しても完了状態が反転しないため
- **領域分離**：トグル/お気に入り/削除ボタンのタップは親要素（編集モード進入トリガー）へ伝播しない
  - 理由：ボタン押下で誤って編集モードに入ると操作コストが跳ねる

#### 優先順位
高

---

### 行削除 UI（品単位） / 長押しメニュー

#### 概要
1 行（1 品）に対する「削除」「上に挿入」「下に挿入」をまとめて行うための **長押しメニュー**。
タップ操作だけで完結する導線を用意する。

本セクションは長押しモードのジェスチャと削除確認ダイアログを定義する。
**メニュー内の `↑+` / `↓+` ボタンの詳細仕様は F013 行間挿入を参照**。

#### 条件

##### 長押しでのモード進入
- 料理名エリアを**長押し（500ms）**すると「長押しメニューモード」（旧称: 削除モード）に入り、対象行がぷるぷる揺れる
  - 理由：妻はテキスト表示を最大限確保したい一方、スワイプ等の独自ジェスチャやページ遷移は避けたい。長押し＋揺れは iOS ホーム画面と同じメンタルモデルで受け入れやすい
- 長押しメニューモードは 1 行のみアクティブ（同時に揺れる行は高々 1 本）
- **空行（EmptyLineItem、★ プレースホルダ）への長押しは無効**とする
  - 理由：空行は「タップで編集開始」を主目的とし、削除・上下挿入のいずれも空行に対しては無意味（削除は内容なし、挿入はその場で編集すれば足りる）

##### モード中の行内 UI（差し替え）
- 通常時の `[✓][料理名][🛒][♡]` を **モード中のみ** `[✓][料理名][↑+][↓+][✕]` に置換する
  - `🛒`（買い物マーカー）と `♡`（お気に入り）は **モード中は非表示**
    - 理由：長押しは「この行への管理操作」モードであり、買い物・お気に入りは「全体タスクへのマーキング」で意図が違う。同時実行を抑制して誤爆を避け、iPhone 11 の行幅 375px を維持する
  - `✓`（完了トグル）は引き続き押せる（完了/未完了切替は管理操作の一種として許容）
  - 料理名エリアはタップで通常モードに戻りつつフロート編集を起動する（既存挙動を維持）
- `↑+` / `↓+` / `✕` の 3 ボタンはいずれも **44×44px**（iOS HIG 準拠、🛒/♡ と同サイズ）

##### `✕`（削除）の挙動
- ✕ タップで画面中央に確認ポップアップ（ダイアログ）を表示する
- ポップアップには対象行のテキストと「削除」「キャンセル」ボタンを表示する
- **キャンセル経路**：ダイアログ背景（オーバーレイ）タップ／「キャンセル」ボタン／Escape キー のいずれでも閉じる
- 「削除」タップで該当行のみ削除し、他行の完了状態は維持される
- その日の最後の 1 行を削除した場合、その日は空日として扱う（meals から除外）

##### モードの解除経路
- 対象行の外をタップ／Escape キー／フロート編集進入／`✕` 確定タップ／`↑+` 確定タップ／`↓+` 確定タップ のいずれでもモードを解除する

#### 優先順位
高

---

### 行間挿入（F013）

#### 概要
既存の料理行の「上」または「下」に新しい料理行を挿入できる。
任意位置への挿入は **既存の長押し（wobble）モードを「メニュー化」して統合**して提供する（行削除 UI / 長押しメニュー セクション参照）。
末尾追加は **末尾行を長押し → `↓+` で行う**（2026-05-21 改訂で行末「＋追加」常設ボタンを廃止。詳細は本セクション末尾「行末追加 UI の廃止（2026-05-21）」参照）。

哲学との整合：常設ボタンを増やさないため **早い** を損なわず、ジェスチャ語彙は既存の長押しと同じため **簡単**、末尾以外の挿入が可能になるため **便利** に寄与する。

#### 条件

##### 起動
- 料理行を 500ms 長押し → 長押しメニューモード（行削除 UI セクション参照）に入る
- 長押しメニューモードの行内 UI は `[✓][料理名][↑+][↓+][✕]` に置換される

##### `↑+`（上に追加）
- タップで対象行の**直上**に空行を挿入し、その空行を FloatingEditor（F011）で**即編集**する
- 長押しメニューモードは即解除
- 挿入時、対象行の `done` / `cart` / 共通お気に入り判定は変化しない
- 挿入された空行は `inCheerWindow` 内なら ★ プレースホルダで描画される（F010 既存挙動）
- 挿入直後のフロート編集起動は F011 既存仕様に従う

##### `↓+`（下に追加）
- タップで対象行の**直下**に空行を挿入し、その空行を FloatingEditor で即編集する
- 長押しメニューモードは即解除
- 末尾行に対する `↓+` は **末尾追加経路の主経路**（2026-05-21 改訂で行末「＋追加」ボタンを廃止したため）
- それ以外は `↑+` と同条件

##### 確定／キャンセル
- フロート編集が起動した後の確定／キャンセルは F011 既存仕様に従う
  - Enter / blur で確定、Escape でキャンセル
  - キャンセルした場合は **空行のまま残る**（自動削除しない、F002「空行も保持可能」原則に従う）
  - 空行の許容は F002「空行も保持可能」原則と整合（残った空行は `inCheerWindow` 内なら ★ プレースホルダで描画され、再タップで編集再開可能。`inCheerWindow` 外なら不可視）
  - 空行への長押しは無効（後述）であるため、内容を入力してから wobble → ✕ で削除する経路を採る。空行のまま残ることは F002 設計の既知挙動として受容する

##### 並び替えとの区別
- 本機能は「空行の位置指定挿入」であり、既存行の順序を入れ替える reorder ではない
- 既存行の上下並び替えは引き続き実装しない（F003 並び替え禁止を維持）
- 理由：reorder は「どこに動かすか」の判断を求めるため **簡単** を損ねる

#### 非対応
- 既存行の上下並び替え（reorder）は実装しない（上記）
- ポップオーバー型のメニュー（テキストラベル選択 UI）は採用しない
  - 既存パターン（行内アイコン置換）に統合する方が **簡単** を損なわない
- 挿入個数の一括指定（「上に 3 行追加」等）は実装しない（ユースケース希薄、**簡単** を損ねる）

#### 行末追加 UI の廃止（2026-05-21）

##### 廃止対象
- 各日リスト末尾に常設配置していた **行末「＋追加」ボタン**（DayRow 内の単独 `<button>＋</button>`）を廃止する
- 廃止根拠：
  - 行が 1 つ以上ある日では、本機能の `↓+`（末尾行長押し → メニュー）が末尾追加と等価。常設ボタンは機能重複でユーザーのメンタル負荷を増やす
  - 常設ボタンを 1 つ増やす変更は CLAUDE.md「常設ボタンを増やす変更は **早い** を損ねる傾向」原則に正面から抵触する
  - DONT.md「使わない機能が画面にあること自体がユーザー体験を悪化させる」基本原則に従い、重複した追加経路は削減する

##### 残存する追加経路
本廃止後も、以下の経路で追加・編集は維持される：
- **既存行が 1 つ以上ある日**：行を長押し → `↑+` / `↓+`（任意位置・末尾とも）
- **today 以降の空日**：空行★プレースホルダ（入力導線）のタップで編集起動
  - today〜today+6 の空日：F010 自動生成 4 空行の ★ をタップ（応援表示と重畳）
  - today+7 以降の空日（lines.length === 0）：**＋マークを 1 つ描画**し、タップで空行 5 行を展開（`bulkAddEmptyLines([date], 5)`）→ 各 ★ をタップしてフロート編集。複数品をまとめて書きやすくする
- **過去日（today 未満）の空日**：能動的な追加 UI は持たない（F012 スワップ + 既存行の長押しメニューで対応）

##### 空日の入力可否（2026-05-31 再評価）
「応援表示ウィンドウ（today〜today+6）」と「入力可能範囲」を **別概念** として分離する。

- **入力可能範囲＝today 以降の全日**：空日でも入力導線を描画し、タップで入力を開始できる
  - 判定は `isInputableDate(date, today)`（`date >= today`、`src/lib/date.ts`）。`DayRow` の `canInput` prop へ
  - today〜today+6 の空日：起動時に自動投入された 4 空行の ★ をタップ（応援表示と重畳）
  - today+7 以降の空日（行データ無し）：**＋マーク**を 1 つ描画。タップで `bulkAddEmptyLines([date], 5)` により空行 5 行を展開し、各 ★ をタップしてフロート編集に入る。未来全域に ★ を散らさず「書きたい日だけ ＋ から開く」ことでプレッシャーを抑えつつ、複数品をまとめて書ける（2026-05-31 ＋マーク方式採用）
- **応援表示（シマエナガ装飾 + 起動時の自動 4 空行投入）は today〜today+6 のまま**（F010、変更なし）
  - 応援を未来全域に拡げると「未計画が大量にある」プレッシャーになり逆効果（F010 非対応条件を維持）。だから応援は 7 日に絞り、入力導線だけを解放する
- **過去日（today 未満）の空日には入力導線を出さない**（据え置き）。既存データの修正は F012 日付ごとスワップ + 既存行の長押しメニュー（編集・削除）で対応

###### 再評価の経緯
- 2026-05-21（PR #40）に「行末『＋追加』ボタン廃止」と同時に「過去日 / today+7 以降の空日への新規追加経路は提供しない（仕様確定）」と確定。根拠は「遠い未来日への先行入力はほぼ発生しない（ユーザー確認）」だった
- 2026-05-31、妻から「6/7 以降（＝当時の today+7 以降）の献立が書けない」報告。**遠い未来日への先行入力ニーズが実在**し、上記前提が覆った
- 旧仕様が予約していた「将来 妻の運用が変化した場合は再評価する」の再評価条件に該当 → 本サイクルで **未来空日の入力導線を解放**（応援表示の範囲は据え置き、＝「入力導線だけ解放」）
- 過去日の解放は今回見送り（報告は未来日のみ・ユーザー選択）。将来 過去日追記の要望が出た場合は同様に再評価する（DONT.md「将来スコープに入る可能性があるもの」参照）
- **2026-05-31（同日・追補）**: 当初実装の「入力起点★を 1 本」では複数品をまとめて書きにくいとのユーザー指摘を受け、**「＋マーク → タップで空行 5 行を展開」方式に変更**。応援ウィンドウ（today〜today+6）の自動 4 空行は据え置き。この ＋マークは「既存行がある日の `↓+`」とは別文脈（空日専用・機能重複なし）であり、2026-05-21 に廃止した行末「＋追加」常設ボタンの復活ではない（DONT.md 却下表に追記）

##### 影響範囲
- `src/components/DayRow.tsx` の `<button onClick={handleAddLine} aria-label="行を追加">＋</button>` ブロックを撤去（本リリースで実施済）
- `DayRowProps.onAddLine` プロップ、`CalendarProps.onRequestAddLine` 経由の伝搬、`App.tsx` の `handleRequestAddLine` ハンドラを撤去（他に呼び出しなし確認済、本リリースで実施済）
- `useAppData.ts` の `addLineAt(date, "end")` API を撤去（本リリースで実施済。末尾追加は F013 `insertLineAt(..., "below")` で代替可能）
- 既存テスト（`tests/DayRow.test.tsx` 等）の「＋追加」ボタン依存テストは「廃止確認」テストへ置換、`tests/StockList.test.tsx` / `tests/useAppData.test.tsx` のモック・テストから `addLineAt` を撤去（本リリースで実施済）

#### 優先順位
中（妻の明示要望、運用上の小さな欠落を埋める）

---

### 日付ごとスワップ（F012）

#### 概要
ある日の献立を別の日と「丸ごと入れ替える」機能。
献立計画の差し替え（例：「火曜の予定を水曜に回して、水曜の予定を火曜に」）を最短手数で行う。
データ消失リスクをゼロにするため**スワップ（双方向入れ替え）方式**を採用し、一方向の「移動（上書き）」「コピー」「削除」は提供しない。

哲学との整合：日付ラベル長押し→目的日タップの 2 アクションで完了するため **早い**、確認ダイアログ不要・既存ジェスチャと同型で **簡単**、計画変更の標準操作を実現するため **便利** に寄与する。

#### 条件

##### 起動・終了
- 日付ラベル（左カラム上部の「M月D日（曜）」表示。祝日名・「今日」バッジを含む領域）を **500ms 長押し**で「移動モード」に入る
  - 同ジェスチャ語彙：料理行の長押し（F004）と揃え、メンタルモデルを共有する
- 移動モードは 1 日のみアクティブ（同時に移動元になれるのは 1 日）
- 移動元の日付ラベルは視覚強調する：背景色 `bg-blue-50` + 細枠 `ring-2 ring-blue-300`
  - 今日（`bg-yellow-50`）と区別できる色で「移動中」を示す
- 移動モードの**解除経路**：移動元と同じ日付ラベルを再タップ／Escape キー／他のジェスチャ（フロート編集起動・料理行 wobble 進入等）
  - F004 wobble の解除経路と同型

##### 目的日タップでスワップ実行
- 移動モード中に**任意の日付ラベル**をタップすると、移動元 A と目的日 B の以下のフィールドを入れ替える：
  - `meals[A].lines ↔ meals[B].lines`（料理行配列、各 `MealLine.done` / `MealLine.cart` を含む）
  - `meals[A].memo ↔ meals[B].memo`（ちょいメモ）
- スワップ後、移動モードは即解除される
- スワップは即時保存（保存ボタンなし、F002 と一貫）
- 移動元と同じ日付ラベルをタップした場合はキャンセル扱い（解除経路と同じ）

##### スワップ対象外
- お気に入り（`AppData.favorites`）：正規化テキスト集合で管理しており、先頭トークン一致は自動追従するため移動不要
- 累積メダル数（`AppData.completedWeeks`）：別途下記「週達成の再評価」で扱う
- ストックリスト：日付に紐付かないため対象外

##### 週達成の再評価
- スワップ後、移動元 A と目的日 B が属する週（日〜土）両方を再評価する
- いずれかの週が**新たに**完成条件を満たした場合：
  - `completedWeeks` に該当週の日曜キーを union（F009 既存ロジック踏襲）
  - **新規達成週のみ**達成演出スプラッシュ（`week-complete.png`、3 秒）を発火する
  - 2 週同時に新規達成した場合は順次表示せず、片方のみ発火する（演出の重複回避、優先度は移動元 A の週）
- いずれかの週が**未達成化**する変動の場合：
  - `completedWeeks` からは**削除しない**（F009「一度入ったキーは減らない（妻の献血カウントセマンティクス）」を維持）
  - 演出も発火しない
- スワップ前後で達成状態が変わらない場合は何も起こらない

##### 視覚フィードバック
- 移動モード中の他の日付ラベルは「タップ可能」を控えめに示す（既存スタイル維持で十分。過度な強調は **簡単** の阻害になる）
- スワップ実行時は両日に薄いフラッシュ演出（150ms 程度）。長尺アニメは不要

##### 空日との関係
- 移動元または目的日が空日（`meals[date] === undefined` または `lines.length === 0 && memo === undefined`）の場合もスワップ可
- 結果として「空日 ↔ 入力日」の入れ替えとなる（=「移動」と同等の見え方）
- 両方が空日の場合はノーオペ（変化なし）。フラッシュ演出も発火しない

##### 過去日・未来日・月またぎ
- 過去日 ↔ 未来日のスワップも許可する（妻が「先週分を整理し直す」等の運用を想定）
- 月またぎ・年またぎも許可
- 移動先の日付ラベルが画面外にある場合：移動モード中は通常通りスクロール可能、モードはスクロールでは解除されない

##### 同時実行制約
- 移動モード中に料理行の長押しが成立した場合：移動モードを解除し、料理行の長押しメニューモードへ
- 移動モード中にフロート編集を起動した場合（行タップ／メモタップ等）：移動モードを解除しフロート編集起動
- 同時に複数の操作モードが並行することはない

#### 非対応
- 日付ごと「移動（上書き）」「コピー」「削除」は提供しない（スワップに一本化、データ消失リスクゼロ）
- 複数日まとめての移動・週ごと移動は提供しない（手数増・メンタル負荷増、**簡単** を損ねる）
- 範囲選択 UI（複数日選択モード）は提供しない
- ドラッグ&ドロップは提供しない
  - 理由：iOS Safari の長押しスクロールと干渉、長距離スクロール中のドロップ誤爆、実装コスト高。タップ 2 回方式で十分
- アンドゥ機能は提供しない（スワップは可逆操作なので「もう一度同じ操作で戻る」で代替）
- 移動モード中の「移動中インジケータ」「説明テキスト」は表示しない（**簡単** 重視、視覚強調のみで伝える）

#### 優先順位
中（妻の明示要望、運用必須ではないが計画変更の手数を大きく下げる）

---

### お気に入りマーカー（料理単位・共通マーキング）

#### 概要
各品（行）に「お気に入り」マーカーをワンタップで付けられる。
「これ良かった」「定番」を本人だけが分かる印として残す用途。
**同じ料理は正規化一致で自動的に共通マーキング**される
（例：ある日の「豚ばら大根」をお気に入りに付けると、別日の「豚バラ大根」にも自動で印が付く）。
強制ではなく、付けなくても全機能が問題なく使える。

#### 条件
- 各行（品）の行末にお気に入りトグルUIを1つ配置する
- タップでお気に入り on/off が即切り替わる
- お気に入りされた行はハートアイコンで視覚的に区別する
- マーカーは**料理名単位**。日単位のまとめお気に入りは配置しない
- 同一料理の判定は**「空白区切りの先頭トークン」を正規化した文字列**で行う
  - 例：`豚バラもやし` / `豚バラもやし 味噌` / `豚ばらもやし` は全て共通キー扱い
  - 正規化は検索と同じ（ひらがな/カタカナ・全角/半角・大小英字）
  - 漢字とかな（「豚」⇔「ぶた」のような変換）は統一しない。検索と挙動を揃える
  - 理由：妻は「料理名 + 補足（味付け・バリエーション）」の順で書く傾向があり、
    先頭を料理名として共有すれば補足違いでも同じ印が付く
- お気に入り状態は完了状態と独立（完了済みでもお気に入り可）
- 行のテキストを変更した場合：旧テキストのキーのまま保持される
  - 変更後のテキストが正規化一致する他の料理のキーと一致すれば自動でマーク表示、一致しなければマーク非表示になる
  - 実装は「行」単位ではなく「正規化テキストの集合」として保持する
- スクロール・アプリ再起動後もお気に入り状態は保持される
- お気に入りの効果は**マーカー表示のみ**。検索フィルタ・並び替え・専用一覧は実装しない
  - 理由：「使わない機能が画面にあること自体が UX を悪化させる」（DONT.md 基本原則）
- 既存データ（favorites フィールドなし）は空配列として読み込み、互換性を維持する

#### 優先順位
中

---

### 過去履歴検索

#### 概要
キーワードを入力すると、そのキーワードを含む／類似する過去の日を新しい順に表示し、各日の献立全体を確認できる。

#### 条件
- 検索欄は画面上部に常時表示される
- 検索結果はカレンダー表示を**邪魔しない**。具体的には：
  - 検索欄が空の状態では結果は表示されない（カレンダーが見える）
  - キーワード入力時のみ、検索欄直下にオーバーレイパネルとして結果を表示する
  - パネル外タップまたは検索欄クリアで結果表示は消え、カレンダーに戻る
- 検索対象は全日付の入力内容
- 検索は以下の正規化を行ったうえで実行する：
  - 大文字/小文字の統一
  - 全角/半角の統一
  - ひらがな/カタカナの相互マッチ（「豚バラ」と「ブタバラ」「ぶたばら」をヒット扱い）
- 検索結果は以下の順に並べる：
  1. 完全一致（正規化後の文字列がキーワードを含む）を日付降順
  2. 類似一致（キーワードの一部を含む、または類似度の高いもの）を日付降順
- 結果は最大20件まで取得し、パネル内でスクロール可能にする
- 各結果は「日付」「その日の全入力内容」を表示する
- 結果をタップすると、該当日が画面中央に来るようカレンダーをスクロールする（パネルは閉じる）

#### 優先順位
高

---

### バックアップ（クリップボード方式 + modal 集約 + swipe-reveal、2026-05-17 改訂3）

#### 概要
妻のデータ（meals / stock / favorites / completedWeeks）消失リスクへの「万が一の保険」として、
**ストックヘッダー上を vertical swipe（≥30px）すると右端から「バックアップ」ボタンが slide-in（3 秒で自動 slide-out）し、タップで中央 modal が開いてその中でクリップボードに JSON をコピー**する方式を採る。
クラウド同期はせず（完全ローカル原則を維持）、ファイル蓄積も催促 UI もない。
妻に何も求めない設計を最上位に置く。

2026-05-17 改訂2 で「ストック折りたたみ内インライン配置」から「単一エントリボタン → 中央 modal」へ変更し、
2026-05-17 改訂3 でさらに「ボタンを常時表示」から「ヘッダー swipe で 3 秒だけ出る」へ変更した。
妻の動作確認で「ストック以外のスクロールが発生」「自然に隠れてほしい」フィードバックを受けた対応で、
バックアップは緊急時の保険であり頻繁に目にする必要がない（普段は完全に隠れる）方が「簡単」を最大化できるという判断。

旧仕様（週番号付きファイル名で iOS Files に蓄積 + 30 日経過のシマエナガバッジ催促）は本リリースで廃止する。
廃止根拠：
- F012 日付ごとスワップでデータ消失リスクが大幅に低減し、強い保険機能の必要性が下がった
- 妻の運用要件 R1（ファイル累積を避けたい）/ R2（万が一の保険のみ、頻度は低い）/ R3（OS ダイアログ・確認の中断感を嫌う）に旧仕様が合わなくなった
- Council 諮問（`history/COUNCIL-LOG.md` 2026-05-17 backup-policy-revision、unanimous、judgment_confidence 0.85）で Option D（クリップボード + 催促廃止 + 復元 UI 維持 + ガイダンス補強）が全会一致で選ばれた

哲学整合：
- **早い** — タップ 1 回で完了、OS ダイアログを挟まない
- **簡単** — 催促 UI なし。妻に「忘れていい」と伝える設計
- **便利** — メモ帳には無い保険経路（JSON 形式での外部保管 + ファイル復元）が機能する

#### 条件

##### エントリ UI（2026-05-17 改訂3 — swipe-reveal）
ストックヘッダー（「ストック（N）▾/▸」と表示されている開閉バー）上で **vertical swipe（≥30px）** を行うと、ヘッダー右端から **「バックアップ」ボタン** が slide-in（200ms、ease-out）し、**3 秒経過で自動的に slide-out** して隠れる。普段は完全に隠れているため、画面占有はゼロ。

- swipe 検出は `pointerdown` → `pointermove` で最大 |dy| を測り、`pointerup` 時点で 30px 以上なら確定する
- swipe 確定後 200ms 以内の `onClick` は無視する（ヘッダー tap によるストック折りたたみトグルと swipe を干渉させないため）
- 移動量 < 30px の `pointerup` は単純な tap として扱われ、ストック折りたたみが開閉する（既存挙動を保つ）
- 自動 hide のタイマー起点は **slide-in 出現時点**（妻側選好：「引っ張って 3 秒後には隠れる感じ」）。出現中にボタンをタップして modal を開いた場合もタイマーは止まらず、3 秒後に backup ボタン自体は slide-out するが、その時点で modal が前面に出ているため画面体験への影響はない
- 「バックアップ」エントリボタンは単独ターゲットのため 44×44px 以上（CLAUDE.md UI 規約）。slide-in 中は `pointer-events: auto`、slide-out 中（`translate-x-full opacity-0`）は親 wrapper 全体が `pointer-events: none` で誤タップを防ぐ
- ボタンタップで **中央 modal**（既存 ConfirmRestoreDialog と同じパターン）を開き、コピー / ファイル復元 / クリップボード貼り付け復元の 3 操作を集約する
- modal を閉じる経路：背景タップ／× ボタン／Escape キー
- 設計判断：バックアップは緊急時の保険（DONT.md「使わない機能を画面に置かない」整合）。普段から見えると「気にすべき何か」のメンタル負荷を生むため、能動的に呼び出した時だけ見える設計が「簡単」（妻のメンタル負荷ゼロ）を最大化する

##### エクスポート UI（コピー）
- modal 内の **「バックアップをコピー」ボタン** がエクスポートのエントリ
- タップで `navigator.clipboard.writeText(JSON.stringify(AppData, null, 2))` を発火する
  - user gesture 起源のため iOS Safari の permission prompt は発生しない
  - 直列化は旧 `serializeBackup` 関数（インデント 2）を流用する
- 書き込み成功時：
  - 画面下部に **インライントースト**「コピーしました。Keep メモやメモ帳に貼り付けて保管してください」を 3 秒表示（自動消去）
  - **modal を自動クローズ**してストック画面に戻す（操作完了の明確な合図 + 一連の動線を最短化）
  - 「Keep メモ」は LINE 内の自分専用チャット。旧「LINE Keep」サービスは終了済のため呼称を改める
  - 哲学者の補強（Council #33 補強案）：「コピー」と「保存」のメンタルモデルを橋渡しする
  - トーストは `pointer-events: none` で操作を妨げない。`prefers-reduced-motion` でフェードを無効化（CSS 側の `@media (prefers-reduced-motion: reduce)` で処理。Tailwind の `motion-safe:` プレフィックスはカスタム class に効かないため使わない）
  - **CLAUDE.md「アニメは 100-200ms 以内」規約の例外**：トーストの 3 秒滞在は narrative animation の枠で許容（メッセージ読了時間の確保）
- 書き込み失敗時（権限拒否・古い iOS 等）：
  - トーストを「コピーに失敗しました」に切り替え、フォールバックなし（妻には負担をかけない）
  - modal は **開いたまま**（リトライしやすい状態を保つ）
- トーストの a11y：`kind === "error"` は `role="alert"` + `aria-live="assertive"`、info は `<output>` の既定 `role="status"` + `aria-live="polite"`

##### 復元 UI（modal 内、2 経路を両方残す）
modal 内に「ファイルから復元」「クリップボードから復元」の 2 つのボタンを縦に並べる。
両経路の本実装は共通の挙動（確認ダイアログ → 検証 → 上書き）に集約する。

###### 経路 1：ファイル復元（旧仕様の互換維持）
- 「ファイルから復元」ボタンタップで `<input type="file" accept="application/json">` を起動
- 過去にダウンロード保存した `cookpato-backup-*.json` ファイルを復元する経路を温存する
  - **互換性保証**: 旧仕様で `<a download>` 経由で書き出された JSON ファイルは本経路で復元可能であることを保証する（妻の端末に過去ファイルが残っている前提）

###### 経路 2：クリップボード貼り付け復元（新規）
- 「クリップボードから復元」ボタンタップで modal 内に **textarea を展開**し、ユーザーが JSON を貼り付ける
  - `navigator.clipboard.readText()` の直接呼び出しは iOS で「貼り付けますか?」プロンプトが出るため採用しない（R3 阻害）
  - textarea + 貼り付けジェスチャの方が iOS 標準操作で習熟済み
- 貼り付け後「復元」ボタンで実行（「キャンセル」で textarea を閉じる、エラー文言もクリア）
- 空 textarea で「復元」タップ時はインラインエラー「貼り付けてください」（modal は開いたまま）
- コピー→貼り付けの対称運用が成立（コピーで保管 → 貼り付けで復元）

##### 復元時の共通挙動（両経路）
- file の読み出し / textarea の貼り付け確定の時点では **まだ `importFromText` を呼ばない**。`pendingText` として保持
- 上層に **確認ダイアログ**「現在のデータを上書きします。続行しますか？」→「復元」「キャンセル」を重ねて表示（z-index 60、本 modal は z-50）
- 「復元」タップで JSON をパースして検証 → 既存の **`parseBackup`**（`src/lib/backup.ts`）を使う。`parseBackup` は内部で `coerceAppData` を呼び出した上で「実質空（lines/stock/favorites/completedWeeks すべて空）」をエラーとして弾くため、malformed JSON や空 JSON で現データが空に上書きされる事故を防ぐ。**`coerceAppData` を直接使ってはならない**（sanitizer であり、不正形状でも `initialData()` を返してしまうため検証用途に不適）。成功時にプライマリ localStorage に書き込み + state 全更新
- 成功時：modal 内に **インライン成功メッセージ「バックアップから復元しました」を 3 秒表示**（modal は開いたまま、ユーザーが結果を視認してから × で閉じる。コピーと異なり「全データ上書き」の重さに比例して明示の確認動線を残す）
- 検証失敗時：modal 内にインラインエラーを表示、現在のデータは変更しない、modal は開いたまま

##### データモデル進化（旧キーの即時削除）
- localStorage キー `cookpato:lastExport:v1` は本リリースで即時削除する
  - 旧仕様の「30 日経過判定」が廃止されたため不要
  - 実装上のコードパス：`src/lib/storage.ts` の **`loadLastExport`** / **`saveLastExport`** と、それを呼び出す **`src/hooks/useBackup.ts`** を撤去する（`src/lib/storage.ts` の `coerceAppData` は AppData プライマリキー `cookpato:data:v1` のみを扱い、`lastExport` キーには触れないため変更不要）
  - 初回起動時の後始末として `localStorage.removeItem('cookpato:lastExport:v1')` を 1 度だけ実行し、妻の端末からも痕跡を消す（idempotent 操作）
  - **expand-contract プロトコル例外条項**：本キーは AppData プライマリではなく「機能廃止と運命を共にするメタ情報」のため、移行先データがなく expand-contract は適用不能。「データモデル進化」セクション末尾「禁止事項」の expand-contract 義務は AppData プライマリキー（`cookpato:data:v*`）に対するもので、本ケースは例外として扱う
- localStorage プライマリキー `cookpato:data:v1` のスキーマは変更なし（互換性 full-compat 維持）

#### 廃止（本リリースで撤去）
- **シマエナガバッジ催促 UI**（旧 PR #29-#30 の歩行アニメ含む全機能）
- **30 日経過判定**（`shouldShowExportBanner` / `lastExport` state 管理）
- **`<a download>` 経由のファイル書き出し**（`triggerDownload` / `getBackupFilename` / ISO 週番号ファイル名生成）
- **localStorage キー `cookpato:lastExport:v1`**（保存・読み取りとも撤去）
- **書き出し時の OS ダイアログ誘導**（クリップボード方式で代替）

#### 非対応（継続）
- クラウド同期・サーバー送信（完全ローカル原則）
- 自動バックアップ（iOS Safari の Periodic Background Sync 非対応 + 妻の運用負荷上昇）
- 同一 localStorage 内の自動二重化（媒体内コピーは ITP・ブラウザクリア・端末交換いずれも共倒れになるため意味がない）
- バックアップ履歴の自動管理（妻の保管先で完結する設計）

#### 優先順位
中（保険機能。F012 スワップでリスクが下がった現状では「強い保険」より「妻の運用負荷ゼロ」を優先）

---

### ストックリスト

#### 概要
冷凍ストック・下味つけ済みの料理等を記録し、画面下部に常時表示する。月をまたいでも保持される。
複数個を作る運用（例：下味豚を3パック）が前提なので、名前と**個数**を別々に入力する。

#### 条件
- 画面下部に固定表示される（カレンダーをスクロールしても常に見える）
- 折りたたみ可能。デフォルトは展開状態
- 項目は「名前（フリー入力）」と「個数（整数、省略時 1）」の組で保持する
  - 例：`下味豚 × 3`、`グラタンのもと × 1`
- 追加 UI：名前入力フィールド + 個数入力フィールド + 追加ボタン
  - 個数を空にして追加すると 1 として扱う
- 行内 UI：`[−][個数][＋][名前]` の並び
  - `−` で個数を 1 減らす（0 でクランプ）
  - `＋` で個数を 1 増やす（使いきり後の再ストック等）
  - 数値表示エリアは個数 ≥ 1 のとき単なるラベル、**個数 = 0 のとき赤い削除ボタン**に変化する
    - 「0 をタップすると削除確定」。押し間違えで 0 にしても `＋` で復帰できる（削除までは 2 アクション）
    - 専用の ✕ 削除ボタンは置かない（「削除ボタンが常時視界にある UI」を避ける）
- 行は**1行に収める**。名前が長い場合はフォントサイズを動的縮小して 1 行で全文表示する
  - 料理行と同じ `useAutoShrink` 機構（Excel の「縮小して全体を表示」相当）を流用
  - 折り返し（複数行）禁止。下限 10px、それ以下になる場合は端を省略してよい
  - 理由：行が複数行に膨れるとリスト全体の視認性が落ちる。料理行と一貫した見た目を保つ
- `[−][個数][＋]` の 3 要素は **36×36px** を採用してよい（行幅確保のため iOS HIG 下限の 44px を例外的に下回る）
  - 理由：3 要素が密接配置で個別ターゲットがあり、行右端ではなく行内中ほどに位置するため誤爆耐性が高い。
    名前領域を最大化する目的で本要素群のみ縮小を許容する。トグル/お気に入り/削除等の単独ボタンは 44px を維持する
  - 個数表示の `tabular-nums` 数字は `w-7`（28px）程度まで縮小可
  - 個数 = 0 時の赤い削除ボタンも同サイズ（36×36px）に揃える
- **名前編集**：行の名前部をタップすると編集モードに入り、テキスト入力フィールドが出現する
  - 編集中は他行の編集モードに干渉しない（同時編集はしない）
  - 確定経路：blur（フォーカスアウト）／Enter キー
  - 空文字確定は無視する（既存の名前を維持。誤タップで全消ししても復元不要）
  - 名前は表示テキスト（`text`）を直接書き換える。お気に入り判定キー（先頭トークン正規化）も自動で追従する
  - IME 多重入力バグ回避のため料理行 textarea と同じ uncontrolled パターンを採る
- **並び替え**：`[↑][↓]` ボタンは廃止し、**長押しドラッグ**で並び替える
  - 行の名前領域を 500ms 長押しすると並び替えモードに突入（料理行の削除モードと同じ `useLongPress` を流用、視覚的に行を浮かせる）
  - 突入後はそのままドラッグ（touchmove / pointermove）で行を上下に移動できる
  - リリース（pointerup / touchend）で位置確定。リリース位置に応じてストック配列を並べ替える
  - 並び替えモード中にドラッグせず指を離した場合は単なる解除（位置変化なし）
  - 並び替えモード中の外タップ／Escape キーでも解除する（料理行の wobble モードと同じ脱出経路）
- 月切替・アプリ再起動後も内容（名前・個数・順序）は維持される
- ストックリストとカレンダーは同一画面内で常に両方見える（別画面にしない）
- カレンダー側で付けたお気に入り（先頭トークン一致）はストック項目にも
  ハートアイコンで**表示のみ**追従する（ストック側でのトグルはしない）
  - 理由：「豚バラもやし」を定番マーク済みで、ストックにも同名があれば
    自然に印が付いていてほしい。マーカーの管理場所を増やさないため、
    設定はカレンダー側に一本化する
- 既存データ（qty フィールドなし）は 1 として読み込み、互換性を維持する

#### 優先順位
高

---

## 制約

### 動作環境
- **主要ターゲット端末：iPhone 11（iOS Safari）**
  - 論理解像度：375×812（Safari表示領域を想定）
  - タッチ操作前提。マウス・キーボード操作は考慮しない
- **配布方式：ホーム画面追加型PWA**
  - Safariで開いた後、「ホーム画面に追加」でアプリ風に使える
  - 初回ロード後は完全オフライン動作（サーバー通信なし）
  - Service Workerによるアプリシェルキャッシュを行う
- iPhone 11以外のiPhone（横長〜iPhone 15Pro Max）でも崩れない最低限のレスポンシブ対応はする
- PC表示・Android専用最適化は不要

### その他制約
- 対象ユーザーは1名（妻）のみ。認証・マルチユーザー機能は実装しない
- 画面遷移なし。全機能が1画面内に収まる
  - 検索結果タップは同一画面内のスクロール移動として扱う（遷移ではない）
- 登録ボタン・保存ボタンは配置しない。全入力は即時保存
- データは**完全にユーザー端末のローカル**に保存する
  - localStorage または IndexedDB を使用（HOWはLayer 1判断）
  - クラウド同期・マルチデバイス同期・サーバーへのデータ送信は一切行わない
  - ローカル完結のバックアップは採用する（→ バックアップ機能セクション参照）。手段は **クリップボードコピー（エクスポート）+ ファイル/クリップボード貼り付け復元** の 2 経路。旧仕様の `<a download>` 経由ファイル書き出しは 2026-05-17 改訂で廃止済（互換のため旧ファイルからの**復元**経路は残す）
- 日本語のみ対応
- ARC: モノリス（固定）

---

## データモデル進化（`schema-evolution.md` 準拠）

### 適用判断
本プロジェクトは ARC=monolith かつ完全ローカル動作だが、以下の理由で本セクションを記入する：
- 妻が実機（iPhone 11）で運用中、既存 localStorage レコードが存在する
- バックアップで書き出した JSON ファイルが端末内に蓄積されており、過去バージョンとの互換性を保つ必要がある

### 現行バージョン
- localStorage プライマリキー: `cookpato:data:v1`
- AppData スキーマバージョン: v1（破壊的変更未発生のため v1 継続）
- ~~最終書き出し記録: `cookpato:lastExport:v1`~~（2026-05-17 F007 改訂で廃止、本リリースで即時削除）

### 互換性ポリシー
**full-compat（完全互換）** を原則とする。
- 新フィールド追加時はオプショナルとし、読み取り側でデフォルト値を注入する
- 既存フィールドの削除・型変更・セマンティクス変更は禁止
- 破壊的変更が必要な場合は `cookpato:data:v2` への移行を計画し、SPEC.md にマイグレーション手順を明記する

### 既存フィールドの互換性実装（実装済み）

| フィールド | 既存データの扱い | 実装箇所 |
|---|---|---|
| `AppData.favorites`（ルート配列） | なし → 空配列として読み込み | `src/lib/storage.ts` `coerceAppData` |
| `StockItem.qty` | なし／不正値 → `1` として読み込み（0 未満は 0 にクランプ） | `src/lib/storage.ts` `coerceStockItem` |
| `AppData.completedWeeks` | なし → 空配列で初期化。保存値と現在 `meals` 由来の既達成週を union（`unionSorted` + `computeAllCompleteWeekSundays`） | `src/lib/storage.ts` `coerceAppData` |
| `DayMeals.memo`（ちょいメモ） | なし／空文字 → `undefined`（オプショナル扱い） | `src/lib/storage.ts` `coerceMeals` |
| `MealLine.cart`（買い物マーカー） | なし → `false` 扱い（`true` のみセット） | `src/lib/storage.ts` `coerceMeals` |

`useAppData.ts` はこれらの coerce 後 `AppData` をロード／保存する薄いフックであり、デフォルト注入の本体は `src/lib/storage.ts` 側にある。

### デプロイ戦略
**versioned-endpoint 相当**（localStorage キーにバージョン番号を明示し、複数バージョンを同時共存可能にする）。
- 新スキーマ導入時は新キー（例: `cookpato:data:v2`）を追加し、`expand-contract` を実施
  1. **expand**: 旧キーから読み、両方に書く（dual-write）
  2. **backfill**: 起動時に旧キーのデータを新キーへコピー
  3. **switch-read**: 読み取りを新キーに切り替え
  4. **contract**: 旧キーを削除（**前提条件**: switch-read 完了後、最低 4 週間の待機期間中にエラー発生ゼロであること）
- バックアップ JSON ファイルは「読み取り互換」のみ保証（妻が過去ファイルから復元できればよい）

### 進行中のマイグレーション
- なし（v1 継続中）

### 禁止事項
- **expand-contract プロトコル外**での既存 localStorage キーの直接削除（妻の端末でデータ消失する）
  - 上記「デプロイ戦略 4. contract」の前提条件を全て満たした場合のみ削除可。それ以外は禁止
  - **例外**: AppData プライマリキー（`cookpato:data:v*`）以外の「機能廃止と運命を共にするメタ情報キー」（例: 2026-05-17 改訂で廃止された `cookpato:lastExport:v1`）は、機能側のコードが同時に撤去されるため、本禁止事項の対象外とする（idempotent な `removeItem` を 1 度実行）
- 互換性を欠く JSON スキーマでのバックアップファイル生成（過去ファイルからの復元が壊れる）
- スキーマレジストリ等の外部サービス依存（完全ローカル原則違反）
